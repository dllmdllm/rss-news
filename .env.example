MINIMAX_API_KEY=your_token_here
# Anthropic-compatible endpoint — no GroupId needed. Default is MiniMax-M2.7;
# override to MiniMax-M3 for the 1M-token context window (same price at this
# project's scale — see CLAUDE.md 設計決定 for the migration notes).
# MINIMAX_MODEL=MiniMax-M3
